# AIOMetadata Addon - Example Environment Variables

# The public hostname (with protocol) where your addon will be accessible
HOST_NAME=https://your-domain.com

# TMDB API Key (required for movie/series metadata)
TMDB_API=your_tmdb_api_key_here

# TVDB API Key (required for series/anime metadata)
TVDB_API_KEY=your_tvdb_api_key_here

# Fanart.tv API Key (optional, for extra artwork)
FANART_API_KEY=your_fanart_api_key_here

# Redis connection string (for caching)
REDIS_URL=redis://localhost:6379

# Database URI (SQLite or PostgreSQL)
# For SQLite (default):
DATABASE_URI=sqlite://addon/data/db.sqlite
# For PostgreSQL:
# DATABASE_URI=postgresql://user:password@localhost:5432/aiometadata

# Addon password (optional, for admin-level protection)
# ADDON_PASSWORD=your_addon_password_here

# Admin key for protected endpoints (optional)
# ADMIN_KEY=your_admin_key_here

# Enable cache warming (recommended for public instances)
ENABLE_CACHE_WARMING=true

# Cache warming interval in minutes
CACHE_WARMING_INTERVAL=30

# Disable all caching (for development only)
# NO_CACHE=true

# Enable self-healing cache (default: true)
# ENABLE_SELF_HEALING=true

# Gemini API Key (optional, for AI-powered search)
# GEMINI_API_KEY=your_gemini_api_key_here

# Redis connection string (for caching)
REDIS_URL=redis://localhost:6379

# Disable all caching (for development only)
# NO_CACHE=true

# Meta and Catalog cache TTLs (in seconds)
# META_TTL=604800
# CATALOG_TTL=86400

# Enable self-healing cache (default: true)
ENABLE_SELF_HEALING=true

# Max retries for temporary errors (self-healing)
CACHE_MAX_RETRIES=2

# Retry delay in milliseconds for self-healing
CACHE_RETRY_DELAY=1000

# Health check interval in milliseconds for cache health monitoring
CACHE_HEALTH_CHECK_INTERVAL=300000

# Threshold for corrupted cache entries before triggering self-healing
CACHE_CORRUPTED_THRESHOLD=10